#include<stdio.h>
#include<conio.h>
int factorial (int);
viod main()
{
 int n;
 clrscr();
 printf("Enter the no:");
 scanf("%d",&n);
 
  getch();
  }
int factorial(int n)
{
int i,fact=1;
for(i=0;i<=n;i++)
 {
  fact=fact*i;
  i++;
  }
  return fact
}
